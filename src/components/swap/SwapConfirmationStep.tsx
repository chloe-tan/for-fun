import { Fragment } from "react";
import FunButton from "../FunButton";

export default function SwapConfirmationStep() {
  return (
    <Fragment>
      <FunButton type="primary" text="Confirm & Swap" />
    </Fragment>
  )
}